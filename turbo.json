{
  "$schema": "https://turborepo.dev/schema.json",
  "globalEnv": [
    "PGBR_DATA",
    "AUTH_SECRET",
    "BASE_URL",
    "NODE_ENV",
    "NEXT_TELEMETRY_DISABLED",
    "DATABASE_URL"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "!.next/dev/**", "dist/**"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    },
    "lint": {
      "dependsOn": ["^lint"]
    }
  }
}
